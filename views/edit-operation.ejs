<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Изменить операцию</title>
</head>
<body>
    <h1>Изменить финансовую операцию</h1>
    
    <form action="/edit-operation/<%= operation.FinOperationID %>" method="POST">
        <div>
            <label for="date">Дата:</label>
            <input type="date" id="date" name="Date" value="<%= operation.Date ? new Date(operation.Date).toISOString().slice(0,10) : '' %>" required>
        </div>
        
        <div>
            <label for="summa">Сумма:</label>
            <input type="number" id="summa" name="Summa" step="0.01" value="<%= operation.Summa %>" required>
        </div>
        
        <div>
            <label for="category">Категория:</label>
            <input type="text" id="category" name="Category" value="<%= operation.Category %>" required>
        </div>
        
        <button type="submit">Сохранить</button>
    </form>

    <button onclick="window.location.href='/'">Назад</button>
</body>
</html>