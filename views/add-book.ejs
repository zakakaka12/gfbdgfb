<!DOCTYPE html>
<html lang="ru">
<head><meta charset="utf-8"><title>Добавить книгу</title></head>
<body>

  <style>
    .all-made{ display: flex; flex-direction: column; gap: 20px;}
    label{color: rgb(0, 0, 0); font-weight: 700; font-size: 18px;}
    body{background-color: rgb(255, 255, 255);}
    input{border-radius: 20px; background-color: rgb(255, 60, 60); border: none; padding: 5px; color: white;}
    select{border-radius: 20px; background-color: transparent; border: solid 1px red; padding: 0.5rem;}
    button{background-color: black; color: white; border-radius: 10px; padding: 0.75rem; cursor: pointer;}
  </style>
  <h1>Добавить книгу</h1>
  <form class="all-made" action="/add-book" method="POST">
    <div class="asas"><label>Название: <input name="title" required></label></div>
    <div><label>Тема: <input name="theme"></label></div>
    <div><label>Автор:
      <select name="authorId">
        <option value="">— выбрать —</option>
        <% authors.forEach(a=>{ %><option value="<%= a.AuthorID %>"><%= a.Name %></option><% }) %>
      </select>
    </label></div>
    <div><label>Жанр:
      <select name="genreId">
        <option value="">— выбрать —</option>
        <% genres.forEach(g=>{ %><option value="<%= g.GenreID %>"><%= g.Name %></option><% }) %>
      </select>
    </label></div>
    <div><label>Издательство:
      <select name="publisherId">
        <option value="">— выбрать —</option>
        <% publishers.forEach(p=>{ %><option value="<%= p.PublisherID %>"><%= p.Name %></option><% }) %>
      </select>
    </label></div>
    <div><button type="submit">Сохранить</button> <button type="button" onclick="window.location.href='/'">Отмена</button></div>
  </form>
</body>
</html>