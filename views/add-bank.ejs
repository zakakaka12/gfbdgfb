<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Добавить банковские реквизиты</title>
</head>
<body>
    <h1>Добавить банковские реквизиты</h1>
    
    <form action="/add-bank" method="POST">
        <div>
            <label for="bank">Банк:</label>
            <input type="text" id="bank" name="Bank" required>
        </div>
        
        <div>
            <label for="valuta">Валюта:</label>
            <input type="text" id="valuta" name="Valuta">
        </div>

        <div>
            <label for="owner">Владелец:</label>
            <select id="owner" name="VladeletsID">
                <option value="">-- выбрать --</option>
                <% (owners || []).forEach(o => { %>
                    <option value="<%= o.VladeletsID %>"><%= o.Imya %> <%= o.Otchestvo || '' %></option>
                <% }) %>
            </select>
        </div>
        
        <button type="submit">Сохранить</button>
    </form>

    <button onclick="window.location.href='/'">Назад</button>
</body>
</html>